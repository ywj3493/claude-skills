<!--
템플릿: 백엔드 설계 문서 (layered-architecture.md)

내비게이션 규칙은 `${CLAUDE_PLUGIN_ROOT}/references/document-order.md` 참조.
백엔드 설계 체인의 첫 문서이므로 이전 링크는 `../../planning/api-interface.md`다.

**일반적인 4계층 설명을 여기 쓰지 않는다.** 계층의 역할과 허용 의존성은
`architecture.md`의 `## 아키텍처 규약`이 한 번만 소유한다. 이 문서는 **이
도메인에서만 참인 것** — 계층별 실제 파일과 클래스, 이 도메인 고유의 규칙,
규약에서 벗어난 지점 — 만 담는다.

도메인이 프로젝트 전체 규약을 그대로 따르고 예외가 하나도 없다면, 이 문서는
계층별 파일 목록 표 하나로 끝나는 것이 정상이다. 분량을 채우려고 규약을
복사하지 않는다.
-->

> [← 인터페이스 계약](../../planning/api-interface.md) | [다음: 도메인 모델 →](domain-model.md)

# 레이어 구조

> **도메인**: <!-- 도메인 이름 -->
>
> **계층**: 백엔드

<!--
역방향 생성 시: 모든 매핑에 `[REF: path:line]`. 파일이 실제로 그 계층에
있다는 근거가 있어야 한다.
순방향 생성 시: 아직 없는 파일의 계획 경로를 쓰고 `## 읽은 소스`를 삭제한다.
-->

---

## 목차

1. [개요](#개요)
2. [계층별 구성 요소](#계층별-구성-요소)
3. [도메인 고유 규칙](#도메인-고유-규칙)
4. [규약 예외](#규약-예외)

---

## 개요

<!--
이 도메인의 계층 구성에서 특기할 점만 2~4문장. 프로젝트 규약과 완전히
동일하면 "프로젝트 규약을 그대로 따른다"고 한 문장으로 쓴다.
-->

**프로젝트 아키텍처 규약**: [아키텍처 · 아키텍처 규약](../../../architecture.md#아키텍처-규약)

---

## 계층별 구성 요소

<!--
향후 구현 스킬이 파일 위치를 정하는 근거 표다. 열 구성을 바꾸지 않는다.

`역할`은 이 파일이 무엇을 하는지 한 구절. "Order 컨트롤러" 같은 동어반복
대신 "주문 생성 요청 수신과 입력 검증"처럼 쓴다.
-->

### Presentation

| 구성 요소 | 경로 | 역할 | 근거 |
|---|---|---|---|
| | | | |

### Application

<!-- 유스케이스 하나당 한 행. 트랜잭션 경계가 여기 있으면 `역할`에 명시한다. -->

| 구성 요소 | 경로 | 역할 | 트랜잭션 경계 | 근거 |
|---|---|---|---|---|
| | | | <!-- O / X --> | |

### Domain

<!-- 애그리거트·엔티티·값 객체·도메인 서비스·리포지토리 인터페이스.
     상세 구조는 domain-model.md가 소유하므로 여기서는 위치만 적는다. -->

| 구성 요소 | 구분 | 경로 | 근거 |
|---|---|---|---|
| | <!-- 애그리거트 / 엔티티 / 값 객체 / 도메인 서비스 / 리포지토리 인터페이스 --> | | |

### Infrastructure

| 구성 요소 | 구분 | 경로 | 대상 | 근거 |
|---|---|---|---|---|
| | <!-- 영속성 어댑터 / 외부 서비스 클라이언트 / 메시징 --> | | | |

---

## 도메인 고유 규칙

<!--
이 도메인에서만 적용되는 규칙. 예: "결제 도메인의 모든 Application 메서드는
멱등키를 받는다", "주문 애그리거트는 리포지토리를 통해서만 로드한다".

프로젝트 전체에 적용되는 규칙이라면 여기가 아니라 architecture.md에 있어야
한다. 없으면 이 섹션을 삭제한다.
-->

| 규칙 | 적용 범위 | 근거 |
|---|---|---|
| | | |

---

## 규약 예외

<!--
`architecture.md`의 허용 의존성 표를 이 도메인이 어기는 지점.

역방향: 사실만 기록한다. 리팩터링을 제안하지 않는다.
순방향: 의도적으로 예외를 두기로 했다면 그 이유를 적는다. 이유 없는 예외는
설계 결함이므로 예외를 두지 말고 설계를 고친다.

없으면 "예외 없음"이라고 적고 표를 삭제한다.
-->

| 출발 | 도착 | 예외 사유 | 근거 |
|---|---|---|---|
| | | | |

---

## 읽은 소스

<!-- 역방향 스킬 전용. 순방향 생성 시 제목까지 통째로 삭제한다. -->

---

## 관련 문서

- [도메인 모델](domain-model.md) — Domain 계층의 상세 구조
- [시퀀스 다이어그램](sequence-diagram.md) — 계층을 가로지르는 호출 흐름

### 참고 문서

- [아키텍처](../../../architecture.md) — 프로젝트 아키텍처 규약
- [인터페이스 계약](../../planning/api-interface.md) — 이 계층이 제공하는 오퍼레이션
- [용어집](../../../glossary.md)

---

## 문서 정보

| 항목 | 값 |
|---|---|
| **작성일** | YYYY-MM-DD |
| **최종 수정일** | YYYY-MM-DD |
| **상태** | 초안 |
| **분석 기준 커밋** | |
| **참고 문서** | |

**버전 이력**:

- 1.0.0 (YYYY-MM-DD): 최초 작성

---
> **전체 문서**
> [요구사항](../../planning/requirements.md) |
> [유저 스토리](../../planning/user-stories.md) |
> [정보 구조](../../planning/information-architecture.md) |
> [유저 플로우](../../planning/user-flows.md) |
> [인터페이스 계약](../../planning/api-interface.md) |
> **레이어 구조** |
> [도메인 모델](domain-model.md) |
> [ERD](erd.md) |
> [시퀀스 다이어그램](sequence-diagram.md) |
> [추적성](../../planning/traceability.md) |
> [테스트 명세](../../verification/test-spec.md)
