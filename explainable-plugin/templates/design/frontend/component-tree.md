<!--
템플릿: 프론트엔드 설계 문서 (component-tree.md)

내비게이션 규칙은 `${CLAUDE_PLUGIN_ROOT}/references/document-order.md` 참조.

**책임 경계 — 이 문서는 구조만 다룬다.**
- 무엇이 무엇을 담는가 → 이 문서
- 언제 그려지고 무엇을 조회하는가 → `render-flow.md`
- 어떤 상태를 공유하는가 → `state-flow.md`

렌더 시점이나 상태 변화를 여기 쓰고 싶어지면 그건 다른 문서의 내용이다.

**프레임워크 중립 어휘**: "컴포넌트" 대신 "UI 단위"를 기본으로 쓰되,
프로젝트가 실제로 쓰는 명칭이 있으면 개요에 밝히고 그 명칭을 따른다.
-->

> [← 라우팅](routing.md) | [다음: 렌더링 흐름 →](render-flow.md)

# UI 구성

> **도메인**: <!-- 도메인 이름 -->
>
> **계층**: 프론트엔드

---

## 목차

1. [개요](#개요)
2. [화면별 구성](#화면별-구성)
3. [공용 UI 단위](#공용-ui-단위)
4. [입출력 계약](#입출력-계약)
5. [합성 규칙](#합성-규칙)

---

## 개요

| 항목 | 값 | 근거 |
|---|---|---|
| **UI 단위 명칭** | <!-- 이 프로젝트가 부르는 이름 --> | |
| **UI 단위 정의 위치** | | |
| **스타일링 방식** | | |

---

## 화면별 구성

<!--
라우트 하나당 한 소절. 라우트 목록은 routing.md가 소유하므로 여기서
반복하지 않고 링크만 한다.

트리는 **소유 관계**를 보인다. 조건부로 나타나는 것은 이름 뒤에 `(조건부)`,
반복되는 것은 `(반복)`을 붙인다. 조건의 내용은 render-flow.md가 소유한다.

각 노드에 FSD 레이어를 괄호로 표시해 슬라이스 경계가 보이게 한다.
-->

### <라우트 또는 화면 이름>

**라우트**: [`/orders/:orderId`](routing.md#라우트-목록)

```text
OrderDetailPage (pages)
├── OrderSummary (widgets)
│   ├── OrderStatusBadge (entities/order)
│   └── PriceBreakdown (entities/order)
├── OrderLineList (widgets)
│   └── OrderLineRow (entities/order) (반복)
└── CancelOrderButton (features/cancel-order) (조건부)
```

| UI 단위 | FSD 레이어·슬라이스 | 역할 | 근거 |
|---|---|---|---|
| | | | |

---

## 공용 UI 단위

<!--
두 개 이상의 화면에서 쓰이는 단위. 한 화면에서만 쓰이면 위의 화면별 구성에
있으면 되고 여기 중복해서 적지 않는다.

`사용처` 열이 비면 그 단위는 실제로 공용이 아니다.
-->

| UI 단위 | FSD 레이어 | 역할 | 사용처 | 근거 |
|---|---|---|---|---|
| | | | | |

---

## 입출력 계약

<!--
UI 단위가 받는 입력과 내보내는 출력. 프레임워크에 따라 props/attributes,
콜백/이벤트/emits 등으로 부르는 것.

**공용 단위와 슬라이스 공개 API만** 적는다. 내부 구현 단위의 입출력까지
적으면 코드가 바뀔 때마다 문서가 틀린다.

타입 표기는 프로젝트가 쓰는 언어의 표기를 그대로 쓴다.
-->

### <UI 단위 이름>

| 입력 | 타입 | 필수 | 설명 |
|---|---|---|---|
| | | | |

| 출력 | 시점 | 전달 값 |
|---|---|---|
| | | |

---

## 합성 규칙

<!--
이 도메인에서 UI 단위를 조립할 때 지키는 규칙. 예: "entities 레이어의 단위는
데이터 조회를 하지 않고 입력으로만 받는다", "features는 반드시 자체 오류
경계를 갖는다".

프로젝트 전체 규칙이면 architecture.md에 있어야 한다. 없으면 이 섹션과 목차
항목을 삭제한다.
-->

| 규칙 | 사유 | 근거 |
|---|---|---|

---

## 읽은 소스

<!-- 역방향 스킬 전용. 순방향 생성 시 제목까지 통째로 삭제한다. -->

---

## 관련 문서

- [라우팅](routing.md) — 어떤 라우트가 이 구성을 그리는지
- [렌더링 흐름](render-flow.md) — 이 단위들이 언제 그려지고 무엇을 조회하는지
- [상태 흐름](state-flow.md) — 단위들이 공유하는 상태
- [FSD 구조](fsd-structure.md) — 레이어와 슬라이스 정의

### 참고 문서

- [용어집](../../../glossary.md)

---

## 문서 정보

| 항목 | 값 |
|---|---|
| **작성일** | YYYY-MM-DD |
| **최종 수정일** | YYYY-MM-DD |
| **상태** | 초안 |
| **분석 기준 커밋** | |
| **참고 문서** | |

**버전 이력**:

- 1.0.0 (YYYY-MM-DD): 최초 작성

---
> **전체 문서**
> [요구사항](../../planning/requirements.md) |
> [유저 스토리](../../planning/user-stories.md) |
> [인터페이스 계약](../../planning/api-interface.md) |
> [FSD 구조](fsd-structure.md) |
> [라우팅](routing.md) |
> **UI 구성** |
> [렌더링 흐름](render-flow.md) |
> [상태 흐름](state-flow.md) |
> [유저 플로우](user-flows.md) |
> [추적성](../../planning/traceability.md) |
> [테스트 명세](../../verification/test-spec.md)
