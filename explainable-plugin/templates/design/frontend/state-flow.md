<!--
템플릿: 프론트엔드 설계 문서 (state-flow.md)

내비게이션 규칙은 `${CLAUDE_PLUGIN_ROOT}/references/document-order.md` 참조.
프론트엔드 체인의 마지막 문서이므로 다음 링크는
`../../planning/traceability.md`다.

**책임 경계 — 이 문서는 공유·전역 상태만 다룬다.**
경계 기준은 "그 라우트를 벗어나도 살아 있는가"다.

- 라우트를 벗어나면 사라지는 지역 상태 → `render-flow.md`
- 라우트를 넘어 살아남는 상태 → 이 문서

이 경계를 지키지 않으면 두 문서가 같은 페이지의 상태를 서로 참조하며
반복한다.

**프레임워크 중립 어휘**: "상태 저장소", "서버 캐시", "선택자", "갱신 동작"
같은 일반 명칭을 쓴다. 라이브러리 고유 명칭은 `architecture.md`의 감지
결과를 인용할 때만 등장한다.
-->

> [← 렌더링 흐름](render-flow.md) | [다음: 추적성 →](../../planning/traceability.md)

# 상태 흐름

> **도메인**: <!-- 도메인 이름 -->
>
> **계층**: 프론트엔드

---

## 목차

1. [개요](#개요)
2. [상태 분류](#상태-분류)
3. [서버 상태](#서버-상태)
4. [클라이언트 공유 상태](#클라이언트-공유-상태)
5. [상태 간 의존](#상태-간-의존)

---

## 개요

| 항목 | 값 | 근거 |
|---|---|---|
| **상태 관리 방식** | | |
| **서버 캐시 계층** | | |
| **영속화 대상** | <!-- 새로고침 후에도 남는 상태와 저장 위치 --> | |

---

## 상태 분류

<!--
어떤 종류의 상태를 어디에 두기로 했는지의 원칙. 이 표가 있어야 새 상태가
생겼을 때 어디에 둘지 판단할 수 있다.

`위치` 열이 비면 그 종류의 상태를 이 프로젝트가 다루지 않는다는 뜻이므로
행을 지운다.
-->

| 종류 | 정의 | 위치 | 수명 |
|---|---|---|---|
| 서버 상태 | 서버가 소유하고 클라이언트는 캐시만 갖는 데이터 | | <!-- 캐시 만료까지 --> |
| 클라이언트 공유 상태 | 여러 라우트가 공유하는 클라이언트 소유 상태 | | |
| 지역 상태 | 한 라우트 안에서만 사는 상태 | 각 라우트 | [렌더링 흐름](render-flow.md) 참조 |

---

## 서버 상태

<!--
`캐시 키`는 render-flow.md의 데이터 조회 표와 일치해야 한다. 어긋나면
어느 쪽이 맞는지 확인하고 고친다.

`무효화 계기`는 캐시가 언제 버려지는지다. 이것이 없으면 "왜 화면이 갱신되지
않는가"를 설명할 수 없다.
-->

| 캐시 키 | 대상 오퍼레이션 | 유효 시간 | 무효화 계기 | 사용 라우트 | 근거 |
|---|---|---|---|---|---|
| | | | <!-- 변경 오퍼레이션 성공, 수동 무효화, 시간 만료 --> | | |

### 낙관적 갱신

<!--
서버 응답 전에 화면을 먼저 바꾸는 경우. 실패 시 되돌리는 방법이 반드시
있어야 한다.
없으면 이 소제목까지 삭제한다.
-->

| 대상 | 갱신 내용 | 실패 시 복구 | 근거 |
|---|---|---|---|
| | | | |

---

## 클라이언트 공유 상태

<!--
여러 라우트에서 읽거나 쓰는 클라이언트 소유 상태. 저장소 단위로 나눠 적는다.

FSD를 따르는 프로젝트에서는 대부분 `entities`나 `shared`의 `model`
세그먼트에 있다. 다른 곳에 있으면 그 사실을 근거와 함께 기록한다.
-->

### <저장소 이름>

| 항목 | 값 |
|---|---|
| **FSD 위치** | |
| **경로** | |
| **영속화** | <!-- 없음 / 로컬 저장소 / 세션 저장소 / 쿠키 --> |
| **근거** | |

**보유 상태**

| 상태 | 타입 | 초기값 | 의미 |
|---|---|---|---|
| | | | |

**갱신 동작**

| 동작 | 계기 | 바뀌는 상태 | 부수 효과 | 근거 |
|---|---|---|---|---|
| | | | <!-- 서버 캐시 무효화, 라우트 이동 등 --> | |

**읽는 쪽**

| 읽는 주체 | 읽는 상태 | 근거 |
|---|---|---|
| | | |

---

## 상태 간 의존

<!--
한 상태가 바뀌면 다른 상태가 따라 바뀌는 관계. 순환이 있으면 그 사실을
반드시 기록한다 — 무한 갱신의 원인이 된다.

없으면 이 섹션과 목차 항목을 삭제한다.
-->

```mermaid
flowchart LR
    AUTH["인증 상태"] --> CART["장바구니 캐시"]
    CART --> BADGE["상단 배지"]
```

| 출발 상태 | 도착 상태 | 전파 방식 | 순환 여부 | 근거 |
|---|---|---|---|---|
| | | <!-- 구독 / 명시적 무효화 / 도출 --> | | |

---

## 읽은 소스

<!-- 역방향 스킬 전용. 순방향 생성 시 제목까지 통째로 삭제한다. -->

---

## 관련 문서

- [렌더링 흐름](render-flow.md) — 라우트별 조회 시점과 지역 상태
- [UI 구성](component-tree.md) — 상태를 읽는 UI 단위
- [FSD 구조](fsd-structure.md) — model 세그먼트의 위치

### 참고 문서

- [인터페이스 계약](../../planning/api-interface.md) — 조회·변경 오퍼레이션
- [유저 플로우](../../planning/user-flows.md) — 플로우 중 유지되는 상태
- [도메인 지도](../../../domain-map.md) — 슬라이스 간 의존

---

## 문서 정보

| 항목 | 값 |
|---|---|
| **작성일** | YYYY-MM-DD |
| **최종 수정일** | YYYY-MM-DD |
| **상태** | 초안 |
| **분석 기준 커밋** | |
| **참고 문서** | |

**버전 이력**:

- 1.0.0 (YYYY-MM-DD): 최초 작성

---
> **전체 문서**
> [요구사항](../../planning/requirements.md) |
> [유저 스토리](../../planning/user-stories.md) |
> [정보 구조](../../planning/information-architecture.md) |
> [유저 플로우](../../planning/user-flows.md) |
> [인터페이스 계약](../../planning/api-interface.md) |
> [FSD 구조](fsd-structure.md) |
> [라우팅](routing.md) |
> [UI 구성](component-tree.md) |
> [렌더링 흐름](render-flow.md) |
> **상태 흐름** |
> [추적성](../../planning/traceability.md) |
> [테스트 명세](../../verification/test-spec.md)
