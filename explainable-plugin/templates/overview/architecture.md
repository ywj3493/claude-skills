<!--
템플릿: 횡단 문서 (architecture.md)

이 문서는 도메인 파이프라인에 속하지 않는 횡단 문서다. 따라서:
- 1행 이전/다음 내비게이션 없음
- 하단 `전체 문서` 인덱스 없음
- `> **도메인**:` 태그 없음

여러 스킬이 공유하는 병합 전용 문서다. 각 섹션의 `<!-- OWNER: -->` 마커가
누가 그 섹션을 쓰는지 정한다. 자기 소유가 아닌 섹션은 **바이트 단위로
보존**한다.

`## 아키텍처 규약` 섹션은 향후 구현 스킬이 파일 위치와 import 방향을 결정하는
근거다. 세 개의 표를 반드시 채운다. 표를 산문으로 대체하지 않는다.
-->

<!-- GENERATED-BY: <스킬명>; commit: <sha>; root: <스캔 경로>; date: <YYYY-MM-DD> -->

# 아키텍처

<!--
역방향 생성 시: 모든 주장에 `[REF: path:line]` 또는
`[ASSUMED: <추론>; basis: <근거>]`를 붙인다.
순방향 생성 시: 인용 없이 사용자가 결정한 값을 기록하고, `## 읽은 소스`
섹션은 통째로 삭제한다.
-->

---

## 목차

1. [개요](#개요)
2. [아키텍처 규약](#아키텍처-규약)
3. [리포지토리 레이아웃](#리포지토리-레이아웃)
4. [백엔드 구조](#백엔드-구조)
5. [프론트엔드 구조](#프론트엔드-구조)

---

## 개요

<!-- OWNER: 먼저 실행한 스킬 -->

<!-- 이 시스템이 무엇인지 3~5문장. 도메인 목록은 domain-map.md가 소유하므로
     여기서 반복하지 않는다. -->

---

## 아키텍처 규약

<!-- OWNER: 먼저 실행한 스킬 (이후 스킬은 자기 계층 행만 추가) -->

<!--
이 섹션은 구현 스킬의 계약이다. 아래 세 표를 반드시 채운다.
표의 열 구성을 바꾸지 않는다 — 기계가 파싱한다.
-->

### 스택

<!--
"근거" 열에는 감지된 경우 `[REF: path:line]`을, 사용자가 정한 경우
"사용자 결정"을 쓴다. 추측한 값은 넣지 않는다.
-->

| 구분 | 선택 | 근거 |
|---|---|---|
| 백엔드 언어·런타임 | <!-- 예: Kotlin 2.0 / JVM 21 --> | <!-- [REF: build.gradle.kts:12] --> |
| 백엔드 프레임워크 | | |
| API 스타일 | <!-- REST / GraphQL / gRPC / 메시지 기반 / 혼합 --> | |
| 진입점 파일 패턴 | <!-- 예: **/*Controller.kt, **/*Resolver.ts --> | |
| 영속성 | <!-- DB 및 매핑 계층 --> | |
| 프론트엔드 언어·런타임 | | |
| 프론트엔드 프레임워크 | | |
| 라우터 | | |
| 상태 관리 | | |
| 렌더링 모드 | <!-- 서버 / 클라이언트 / 사전 생성 / 혼합 --> | |
| 빌드 도구 | | |
| 테스트 도구 | | |

### 레이어·슬라이스 ↔ 디렉터리 매핑

<!--
백엔드는 4-Layered DDD 계층, 프론트엔드는 FSD 레이어를 행으로 쓴다.
한쪽만 있는 프로젝트는 해당 행만 채우고 나머지는 삭제한다.
"실제 경로"는 반드시 실존하는 경로여야 한다.
-->

| 계층 | 구분 | 실제 경로 | 책임 |
|---|---|---|---|
| Presentation | 백엔드 | <!-- src/main/kotlin/.../api/ --> | 외부 요청 수신, 입출력 변환 |
| Application | 백엔드 | | 유스케이스 오케스트레이션, 트랜잭션 경계 |
| Domain | 백엔드 | | 불변식, 비즈니스 규칙, 도메인 이벤트 |
| Infrastructure | 백엔드 | | 영속성·외부 시스템 어댑터 |
| app | 프론트엔드 | | 진입점, 전역 프로바이더, 라우터 설정 |
| pages | 프론트엔드 | | 라우트 단위 화면 조립 |
| widgets | 프론트엔드 | | 재사용 가능한 화면 블록 |
| features | 프론트엔드 | | 사용자 행동 단위 |
| entities | 프론트엔드 | | 도메인 개체와 그 표현 |
| shared | 프론트엔드 | | 도메인 무관 공용 코드 |

### 허용 의존성 방향

<!--
"허용"은 O, "금지"는 X로만 표기한다. 조건부 허용은 비고에 적는다.
역방향 생성 시 실제 코드가 이 규칙을 어기고 있으면, 규칙을 코드에 맞춰
고치지 말고 `## 백엔드 구조` / `## 프론트엔드 구조`의 위반 목록에 기록한다.
-->

| 출발 | 도착 | 허용 | 비고 |
|---|---|---|---|
| Presentation | Application | O | |
| Application | Domain | O | |
| Application | Infrastructure | O | <!-- 인터페이스를 통해서만 --> |
| Domain | Infrastructure | X | 의존성 역전으로 해결 |
| Domain | Application | X | |
| 상위 FSD 레이어 | 하위 FSD 레이어 | O | app > pages > widgets > features > entities > shared |
| 같은 FSD 레이어의 다른 슬라이스 | | X | 공유가 필요하면 하위 레이어로 내린다 |

---

## 리포지토리 레이아웃

<!-- OWNER: 먼저 실행한 스킬 -->

<!--
모노레포면 앱/패키지 경계를 먼저 보이고, 그 아래에 소스 트리를 보인다.
전체 파일 목록이 아니라 **경계가 드러나는 수준**까지만 적는다.
-->

```text
<!-- 디렉터리 트리 -->
```

| 경로 | 역할 | 근거 |
|---|---|---|
| | | |

---

## 백엔드 구조

<!-- OWNER: init-design-backend / reverse-design-backend -->
<!-- 백엔드가 없는 프로젝트에서는 이 섹션을 통째로 삭제한다. -->

### 실측 레이어 패턴

<!--
역방향: 코드가 실제로 어떤 계층 구조인지 먼저 기술하고, 그 다음 4-Layered
DDD와 대비한다. 코드가 3계층이면 3계층이라고 쓴다 — 4계층으로 재분류하지
않는다.
순방향: 이 소제목을 "채택 레이어 구조"로 바꾸고 결정 사항을 기술한다.
-->

### 모듈 경계

| 모듈 | 책임 | 경계를 정하는 근거 |
|---|---|---|
| | | |

### 4-Layered DDD 대비

| DDD 계층 | 이 코드베이스의 대응 | 일치 여부 |
|---|---|---|
| Presentation | | <!-- 일치 / 부분 / 없음 --> |
| Application | | |
| Domain | | |
| Infrastructure | | |

### 의존성 규칙 위반

<!--
`## 아키텍처 규약`의 허용 의존성 표를 어기는 실제 코드가 있으면 여기 적는다.
없으면 "발견되지 않음"이라고 적고 표를 지운다. 리팩터링을 제안하지 않는다 —
사실만 기록한다.
-->

| 출발 | 도착 | 근거 |
|---|---|---|
| | | |

---

## 프론트엔드 구조

<!-- OWNER: init-design-frontend / reverse-design-frontend -->
<!-- 프론트엔드가 없는 프로젝트에서는 이 섹션을 통째로 삭제한다. -->

### 감지된 프레임워크 환경

<!--
프레임워크·라우터·상태 라이브러리·렌더링 모드와 그 근거. 이 표의 값이
`render-flow-tracer` 에이전트 호출의 입력이 되므로 정확해야 한다.
-->

| 항목 | 값 | 근거 |
|---|---|---|
| 프레임워크 | | |
| 라우트 정의 파일 패턴 | | |
| 상태 관리 | | |
| 데이터 조회 계층 | | |
| 렌더링 모드 | | |

### FSD 적합도

<!--
실제 디렉터리를 FSD 레이어에 대응시킨다. FSD를 따르지 않는 프로젝트면
"FSD 아님"이라고 명시하고 실제 구조를 기록한 뒤 대비표만 남긴다.
억지로 FSD로 재분류하지 않는다.
-->

| FSD 레이어 | 이 코드베이스의 대응 | 일치 여부 |
|---|---|---|
| app | | |
| pages | | |
| widgets | | |
| features | | |
| entities | | |
| shared | | |

---

## 읽은 소스

<!--
역방향 스킬 전용. 이 문서를 쓰면서 실제로 연 파일(부분만 읽었으면 줄 범위
포함)을 전부 나열한다. 위의 모든 `[REF: path:line]`은 여기 있는 파일을
가리켜야 한다.
순방향 생성 시에는 이 섹션(제목 포함)을 통째로 삭제한다.
-->

---

## 관련 문서

- [인프라](infrastructure.md) — 배포 토폴로지, 환경, 외부 서비스
- [도메인 지도](domain-map.md) — 도메인 간 관계와 횡단 흐름
- [용어집](glossary.md) — 한국어 용어와 영문 식별자 대응
- [문서 인덱스](README.md)

---

## 문서 정보

| 항목 | 값 |
|---|---|
| **작성일** | YYYY-MM-DD |
| **최종 수정일** | YYYY-MM-DD |
| **상태** | 초안 |
| **분석 기준 커밋** | <!-- 역방향 전용: git rev-parse HEAD 결과. 순방향은 "해당 없음" --> |
| **참고 문서** | |

**버전 이력**:

- 1.0.0 (YYYY-MM-DD): 최초 작성
