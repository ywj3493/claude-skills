<!--
템플릿: 도메인 기획 문서 (traceability.md)

내비게이션 규칙은 `${CLAUDE_PLUGIN_ROOT}/references/document-order.md` 참조.
이 문서는 설계 문서 **뒤에** 온다 — FLOW 키가 있어야 조인이 가능하기 때문이다.
기획 단계에서 먼저 만들어 두고 설계 단계에서 갱신하되, 읽는 순서는 설계 뒤다.

이 문서는 **조인 테이블**이다. 다른 문서의 내용을 옮겨 적지 않는다. ID와
링크만 있고 설명은 없어야 한다. 설명을 쓰고 싶어지면 그건 원본 문서에
있어야 할 내용이다.

`scripts/check-docs.sh`가 이 파일을 파싱한다. 표의 열 구성을 바꾸지 않는다.

이전 링크는 생성된 설계 체인의 마지막 문서를 가리킨다. 프론트엔드 설계가
있으면 `state-flow.md`, 백엔드만 있으면
`../design/backend/sequence-diagram.md`다.
-->

> [← 프론트엔드 설계](../design/frontend/state-flow.md) | [다음: 테스트 명세 →](../verification/test-spec.md)

# 추적성 매트릭스

> **도메인**: <!-- 도메인 이름 -->

요구사항부터 테스트까지의 연결을 한 곳에서 확인하는 문서다. 어느 칸이든
비어 있으면 그 자체가 결함 신호다.

---

## 목차

1. [요구사항 → 스토리](#요구사항--스토리)
2. [스토리 → 화면·플로우](#스토리--화면플로우)
3. [스토리 → 흐름](#스토리--흐름)
4. [인수 기준 → 테스트](#인수-기준--테스트)
5. [미연결 항목](#미연결-항목)

---

## 요구사항 → 스토리

<!--
모든 FR은 최소 하나의 US로 커버되어야 한다. 커버되지 않는 FR은 `## 미연결
항목`으로 내린다.
NFR은 스토리로 커버되지 않는 것이 정상이다 — 테스트로 직접 연결된다.
-->

| 요구사항 | 유저 스토리 | 인수 기준 범위 | 상태 |
|---|---|---|---|
| [FR-<영역>-01](requirements.md#fr-영역-01-제목) | [US-01](user-stories.md#us-01-제목) | AC-US01-01 ~ AC-US01-02 | <!-- 계획됨 / 구현됨 --> |

---

## 스토리 → 화면·플로우

<!--
**조건부 섹션이다.** `information-architecture.md`와 `user-flows.md`를 만든
프로젝트에서만 생성한다. UI가 없으면 이 섹션과 목차 항목을 통째로 삭제한다.

아래 세 표와 달리 이 표는 **기획 단계에서 전부 채울 수 있다** — `SCR-`과
`UF-`는 설계 전에 부여되기 때문이다.

한 스토리가 여러 플로우로 나뉘면 행을 나눠 쓴다.
-->

| 유저 스토리 | 유저 플로우 | 화면 | 상태 |
|---|---|---|---|
| [US-01](user-stories.md#us-01-제목) | [UF-<도메인>-01](user-flows.md#uf-도메인-01-플로우-이름) | SCR-<영역>-01 | <!-- 계획됨 / 구현됨 --> |

---

## 스토리 → 흐름

<!--
`흐름 키`는 설계 단계에서 부여된 `FLOW-` 키다. 여러 도메인을 지나는 흐름은
`FLOW-CROSS-` 키를 쓰고 domain-map.md를 가리킨다.

프론트엔드와 백엔드가 모두 있는 스토리는 두 행으로 나눠 쓴다.
-->

| 유저 스토리 | 오퍼레이션 ID | 흐름 키 | 계층 | 설계 문서 |
|---|---|---|---|---|
| US-01 | | `FLOW-<도메인>-...` | <!-- 백엔드 / 프론트엔드 --> | [시퀀스 다이어그램](../design/backend/sequence-diagram.md) |

---

## 인수 기준 → 테스트

<!--
모든 AC는 최소 하나의 테스트로 커버되어야 한다.
`상태`가 `미구현`이면 테스트 코드가 아직 없다는 뜻이다 — 역방향에서는 흔한
정상 상태이며, 없는 테스트를 지어내지 않는다.
-->

| 인수 기준 | 테스트 ID | 테스트 유형 | 상태 |
|---|---|---|---|
| AC-US01-01 | [T-101](../verification/test-spec.md#백엔드-테스트-매트릭스) | | <!-- 구현됨 / 미구현 --> |

---

## 미연결 항목

<!--
연결이 끊긴 곳을 숨기지 않고 드러내는 섹션이다. 전부 비어 있으면 각 표를
지우고 "미연결 항목 없음"이라고 적는다.

역방향에서 `코드에만 있는 동작`은 특히 중요하다 — 기획에 없던 기능이
구현되어 있다는 뜻이며, 사람이 판단해야 할 사안이다.
-->

### 스토리로 커버되지 않은 요구사항

| 요구사항 | 사유 |
|---|---|

### 흐름이 없는 스토리

| 유저 스토리 | 사유 |
|---|---|

### 플로우에 등장하지 않는 화면

<!--
UI가 있는 프로젝트 전용. 어느 `UF-`에도 나오지 않는 `SCR-`은 도달 경로가
없는 화면이거나 플로우가 빠진 것이다. 소제목까지 삭제하지 말고 비어 있으면
"없음"이라고 적는다.
-->

| 화면 | 사유 |
|---|---|

### 테스트가 없는 인수 기준

| 인수 기준 | 사유 |
|---|---|

### 요구사항에 없는 코드 동작

<!-- 역방향 전용. 코드에는 있으나 어느 요구사항으로도 설명되지 않는 동작. -->

| 동작 | 근거 |
|---|---|

---

## 관련 문서

- [요구사항](requirements.md)
- [유저 스토리](user-stories.md)
- [정보 구조](information-architecture.md)
- [유저 플로우](user-flows.md)
- [인터페이스 계약](api-interface.md)
- [테스트 명세](../verification/test-spec.md)

### 참고 문서

- [도메인 지도](../../domain-map.md) — 도메인 횡단 흐름의 조인

---

## 문서 정보

| 항목 | 값 |
|---|---|
| **작성일** | YYYY-MM-DD |
| **최종 수정일** | YYYY-MM-DD |
| **상태** | 초안 |
| **분석 기준 커밋** | |
| **참고 문서** | |

**버전 이력**:

- 1.0.0 (YYYY-MM-DD): 최초 작성

---
> **전체 문서**
> [요구사항](requirements.md) |
> [유저 스토리](user-stories.md) |
> [정보 구조](information-architecture.md) |
> [유저 플로우](user-flows.md) |
> [인터페이스 계약](api-interface.md) |
> [백엔드 설계](../design/backend/layered-architecture.md) |
> [프론트엔드 설계](../design/frontend/fsd-structure.md) |
> **추적성** |
> [테스트 명세](../verification/test-spec.md)
