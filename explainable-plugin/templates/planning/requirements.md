<!--
템플릿: 도메인 기획 문서 (requirements.md)

내비게이션 규칙은 `${CLAUDE_PLUGIN_ROOT}/references/document-order.md`를
읽고 적용한다. 이 문서는 도메인 체인의 첫 문서이므로 이전 링크가 없다.

**기술 중립 문서다.** 다음 스왑 테스트를 문장 단위로 통과해야 한다:
Python을 Java로, REST를 GraphQL로, PostgreSQL을 MongoDB로 바꿔도 문장이
그대로 참이어야 한다. 프레임워크 이름, API 경로, 타입 시그니처, 테이블
이름은 여기 들어오지 않는다.

`[REF: path:line]` 인용은 예외다 — 기술 내용이 아니라 출처 표시다.
-->

> [다음: 유저 스토리 →](user-stories.md)

# 요구사항

> **도메인**: <!-- 도메인 이름 -->

<!--
역방향 생성 시: 모든 요구사항에 `[REF: path:line]` 또는
`[ASSUMED: <추론>; basis: <근거>]`. 요구사항은 **코드가 강제하는 것**에서
뽑는다 — 검증 로직, 권한 체크, 비즈니스 규칙 분기, 타임아웃·재시도 설정.
"엔드포인트가 존재한다"는 요구사항이 아니다.

순방향 생성 시: 인용 없이 사용자와 합의한 내용을 기록하고 `## 읽은 소스`
섹션을 통째로 삭제한다.
-->

---

## 목차

1. [개요](#개요)
2. [이해관계자](#이해관계자)
3. [기능 요구사항](#기능-요구사항)
4. [비기능 요구사항](#비기능-요구사항)
5. [제약 사항](#제약-사항)
6. [범위 밖](#범위-밖)

---

## 개요

### 목적

<!-- 이 도메인이 해결하는 문제. 3~5문장. 해결 방법이 아니라 문제를 쓴다. -->

### 범위

<!-- 이 도메인이 책임지는 것의 경계. 다른 도메인과의 경계가 애매하면
     domain-map.md의 통합 계약을 참조한다. -->

---

## 이해관계자

<!--
`구분`은 주 액터(직접 목표를 달성하는 주체) / 부 액터(시스템이 의존하는
외부 주체) 중 하나.
사람이 아닌 주체(배치 스케줄러, 외부 시스템)도 액터가 될 수 있다.
-->

| 액터 | 구분 | 목표 | 근거 |
|---|---|---|---|
| | | | |

---

## 기능 요구사항

<!--
ID 형식: `FR-<영역>-NN`. `<영역>`은 대문자 약어(AUTH, ORDER, PAY),
`NN`은 01부터 빈 번호 없이 순차.

한 요구사항은 한 문장으로 검증 가능해야 한다. "~를 잘 처리한다" 같은
문장은 요구사항이 아니다.
-->

### FR-<영역>-01: <제목>

<!-- 시스템이 무엇을 해야 하는지 한 문단. -->

| 항목 | 내용 |
|---|---|
| **우선순위** | <!-- P0 / P1 / P2 --> |
| **관련 액터** | |
| **근거** | <!-- [REF: path:line] 또는 [ASSUMED: ...; basis: ...] 또는 "기획 결정" --> |

**세부 규칙**:

<!-- 검증 조건, 허용 범위, 분기 규칙. 각 항목에 근거를 붙인다. -->

-

---

## 비기능 요구사항

<!--
ID 형식: `NFR-<분류>-NN`. 분류 예: PERF(성능), SEC(보안), AVAIL(가용성),
SCALE(확장성), OPS(운영), A11Y(접근성), I18N(국제화).

**측정 가능한 수치가 없으면 NFR이 아니다.** 역방향에서는 타임아웃, 재시도
횟수, 레이트 리밋, 캐시 TTL, 인덱스, 커넥션 풀 크기, SLO 설정에서 뽑는다.
설정 파일에 값이 있으면 그 값을 그대로 쓰고 인용한다.
-->

### NFR-<분류>-01: <제목>

| 항목 | 내용 |
|---|---|
| **측정 기준** | <!-- 무엇을 어떻게 재는가 --> |
| **목표값** | <!-- 구체적 수치 --> |
| **근거** | |

---

## 제약 사항

<!--
바꿀 수 없는 조건. 법규, 기존 시스템과의 호환, 조직 정책, 마감.
"기술 스택이 X다"는 제약이 아니라 결정이다 — architecture.md에 적는다.
-->

| 제약 | 유형 | 영향 | 근거 |
|---|---|---|---|
| | <!-- 법규 / 호환성 / 정책 / 일정 --> | | |

---

## 범위 밖

<!--
명시적으로 하지 않기로 한 것. 이 섹션이 비어 있으면 범위가 정해지지 않은
것이다. 역방향에서는 코드에 없는 기능 중 "있을 법한데 없는" 것을 여기
기록하되, `[ASSUMED: ...]`로 표시한다.
-->

-

---

## 읽은 소스

<!--
역방향 스킬 전용. 이 문서를 쓰면서 실제로 연 파일을 전부 나열한다.
위의 모든 `[REF: path:line]`은 여기 있는 파일을 가리켜야 한다.
순방향 생성 시에는 이 섹션(제목 포함)을 통째로 삭제한다. 순방향 문서에
`## 읽은 소스`를 넣고 싶어진다면, 그건 기존 코드를 문서화하고 있다는
신호이므로 역방향 스킬을 써야 한다.
-->

---

## 관련 문서

- [유저 스토리](user-stories.md) — 이 요구사항을 사용자 관점으로 푼 것
- [정보 구조](information-architecture.md) — 요구사항이 드러나는 화면
- [추적성 매트릭스](traceability.md) — 요구사항 ↔ 스토리 ↔ 흐름 ↔ 테스트

### 참고 문서

- [아키텍처](../../architecture.md) — 코드베이스 구조와 아키텍처 규약
- [인프라](../../infrastructure.md) — 배포와 환경
- [도메인 지도](../../domain-map.md) — 다른 도메인과의 관계
- [용어집](../../glossary.md) — 용어 정의

---

## 문서 정보

| 항목 | 값 |
|---|---|
| **작성일** | YYYY-MM-DD |
| **최종 수정일** | YYYY-MM-DD |
| **상태** | 초안 |
| **분석 기준 커밋** | <!-- 역방향 전용. 순방향은 "해당 없음" --> |
| **참고 문서** | |

**버전 이력**:

- 1.0.0 (YYYY-MM-DD): 최초 작성

---
> **전체 문서**
> <!-- 실제 생성된 문서만 남긴다. 현재 문서는 굵게, 링크하지 않는다. -->
> **요구사항** |
> [유저 스토리](user-stories.md) |
> [정보 구조](information-architecture.md) |
> [유저 플로우](user-flows.md) |
> [인터페이스 계약](api-interface.md) |
> [백엔드 설계](../design/backend/layered-architecture.md) |
> [프론트엔드 설계](../design/frontend/fsd-structure.md) |
> [추적성](traceability.md) |
> [테스트 명세](../verification/test-spec.md)
